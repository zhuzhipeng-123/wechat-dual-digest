# 第一版最小 MVP：双日报开发执行提示词

你是本项目的编码执行者。请把已经确定的最小方案做成能实际使用的工具，不重新设计通用平台，不用一批空接口冒充完成。

## 1. 目标与范围

工作目录：`C:/Users/capygsq/Desktop/keep learning/wechat_相关`。

第一版只实现这条完整链路：

配置公众号与时间 → 微信读书登录和搜一搜发现 → 获取原文及精确时间 → 入窗、净化、保存图片 → 练习直接生成日报；招聘经过本地 OCR、Agnes 抽取、证据检查和代码评级 → 两份本地 HTML/JSON → 每日定时执行、状态与失败可见。

运行模型固定为 `agnes-2.5-flash`，唯一服务地址 `https://apihub.agnes-ai.com/v1`，使用 Chat Completions。你作为编码模型的身份不改变成品里的模型配置。

首版不做：MCP、RSS、Markdown 导出、FastAPI/REST API、网页管理后台、通用公众号/文章搜索平台、认证主体展示、图片代理、通用文章数据库、向量库、Agent 编排或云部署。这些在当前主链路没有消费者，不能以“补齐架构”为理由重新加回来。

内部函数接口不等于 HTTP API。用一个 Python 程序直接调用各模块，静态 HTML 就是报告界面；简单 Windows 启动入口负责登录、预览、定时运行和打开报告。

## 2. 先读资料，确认实际起点

依次读取：

1. 本项目 AGENTS.md（如果已有）。
2. EXECUTION_PLAN.md：这是最新的第一版范围、流程和验收依据。
3. 当前目录匹配 `wechat-download-api-*.md` 的产品规格，核对正文标题为“微信公众号双日报项目：无源码重建规格”。其中的业务规则仍适用，MCP/RSS 等后置功能和旧的全产品完成条件不属于首版验收。
4. 只读参考 `C:/Users/capygsq/Desktop/keep learning/every_DAY/app/adapters/agnes.py`、同项目的 `app/config.py`、`app/services/model_json.py`、`tests/test_agnes.py` 和空白 `.env.example`。

不要读取或复制参考项目的真实 .env、Cookie、数据库、会话或用户数据，不要修改参考项目。旧“给 GPT-6”提示词只作历史背景，不覆盖本轮最小范围，也不要求切换你的编码模型。

Git 已初始化，先检查状态并保留现有文件。如果接手时已有代码，先读后续做，不清空重建。未经明确授权，不执行 git add、commit、push、发布或向他人发送报告。

此交接时尚未实现业务，也未验证真实微信或 Agnes。方案中的接口、命令和测试只是计划，不能声称已存在或通过。

## 3. 开工规则与一次性确认

如果正式 AGENTS.md 尚不存在或未确认，使用 EXECUTION_PLAN.md 的草稿，向用户集中展示项目目标、目录、技术栈、编码规范和拟定运行方式，确认后才写正式规则并开发。

同时提出这些运行默认建议，不冒充已确认：

- 首版时区 Asia/Shanghai，不承诺通用 DST 地区。
- 按原规格“企业全部校招机会中的最优城市”评级，不擅自只看 AI 岗位城市。
- 手动生成默认隔离预览，不占用或覆盖正式定时任务。
- 调度时间修改次日生效，已认领任务的窗口和配置不变。
- 招聘开放状态以目标窗口截止时刻和原文证据判断。

若当前用户已明确同意这些规则，不重复询问。确认前可做只读核查；确认后持续推进，不每个小步骤都问是否继续。只有真实登录/权限、外部调用授权或影响结果的产品冲突才需要用户参与。

首个可运行版本生成 README.html，按用户要求使用 frontend-design skill，说明实际功能、安装和使用方式。工具环境缺少该 skill 时说明缺口，不假装用过，也不阻断独立工作。实质性行为变更同步更新 AGENTS.md 和 README.html。

## 4. 依次完成三个阶段

### A：先验证核心数据能拿到

- 准备 Python 3.12 项目环境、锁文件、忽略规则、配置样例和最小数据契约。
- 用一个招聘号、一个练习号和已知原文验证微信读书登录、公众号文章搜一搜及候选链接。
- 不猜接口、选择器、账号标识或时间字段；依据当前真实页面/响应实现。
- 先验证候选原文 URL 能否直接取得正文、图片和精确发布时间。不需要公众平台会话就不增加第二套登录；如果实际取文方式确实依赖它，再实现独立扫码及权限检查。
- 公共页面读取失败不是浏览器不可用的证据，扫码成功也不证明所有后台权限都具备。
- 获得外部调用授权后，使用合成文本做 Agnes 最小 JSON 请求，核对实际响应、模型、错误和用量。

A 的真实通过条件：至少一篇已知文章完成“发现 → 原文 → 精确时间核对”；相关登录/权限失败能准确识别。否则标失败或未验证，继续不依赖外部服务的离线工作，不能假造已跑通。

### B：先产出可读的两份报告

实现公众号配置、练习标题筛选、候选去重、时间入窗、正文净化、图片与证据保存。

先完成练习完整报告和招聘无模型降级报告，输出对应 JSON。提供一个简单启动入口，先用控制台菜单即可，不建设网页后台。

B 的通过条件：有真实文章的报告能打开；窗口、来源、图片和状态正确；模型不可用时招聘仍有原文入口和“分析待补充”，不能误报没有招聘。

### C：让完整最小工具日常可用

接入本地 OCR、Agnes 事实抽取、来源/证据/覆盖检查、代码评级和必要筛选；再实现每日调度、持久去重、总超时、版本一致发布和安装说明。

C 的通过条件：真实图片 OCR、真实招聘分析、真实练习报告、实际定时运行和故障演练均通过。MCP/RSS 等不在本阶段偷偷补回。

每个阶段内部按一个可独立验证的增量推进：读相关文件 → 明确可观察期望 → 最小实现 → 运行检查 → 修正 → 更新状态 → 下一步。实现错误先修复，不能在错误上继续堆功能。

## 5. 数据与内容约束

统一维护运行、候选、文章、图片/文字证据、抽取结果、报告和错误状态。不要为这几个对象搭通用框架。

- 运行冻结 task kind、target date、window start/end、timezone、config snapshot、run_id。
- 日报候选只来自微信读书；手动 URL 仅可用于隔离测试，不替代生产发现。
- 招聘不按标题提前过滤；练习任一包含词命中且无排除词命中才处理正文。按账号配置顺序展示。
- 原文精确时间满足 `start <= publish_ts < end` 才入窗。相对时间只作候选提示，不足以生成精确时间。
- 时间未知的失败候选放待核实区域，不计为正式入窗文章；单篇/单账号失败不阻断其他结果。
- 搜索、抓取、分析和覆盖状态分开。没有搜索结果写“本次未发现”，不写“今天没有发文”。
- 练习不 OCR、不调用模型、不补题解题；展示净化正文和可用图片。
- 招聘只提取校招全职机会；正确处理纯实习/社招、宣讲/结果公告、混合表格及旧公告中的新增补招。
- 原文有校招全职和投递指引可作为开放证据；没写截止日不自动等于关闭，也不证明永远有效。未知保持待核实。
- 所有校招岗位都保留，不因偏好删除。企业性质、规模、岗位、地点不靠常识补。
- 保留企业→岗位→地点→证据的关联。北京销售、郑州算法不能写成北京算法。
- 一次模型请求围绕一篇文章或一个证据块，程序核对完整清单后合并，最终报告按公众号分组。
- P0–P3 完整执行原规格矩阵，只有全部已知岗位都明确非目标方向才额外降级。不明岗位不能随意降。
- 模型不写 P 级、推荐结论、自然段或 HTML；代码从同一份已校验事实生成它们。
- 每篇保留唯一安全原文入口。招聘原图只保留作证据，不嵌最终报告；练习保留可用图。

## 6. 模型与证据处理

- 图片在本机 OCR。Agnes 输入仅限本次文章必要来源字段、纯文本、OCR 文字及分段上下文；不发送原图、Cookie、Key或本机绝对路径。
- 顺序检查：完整响应 → JSON → 严格 Schema → 来源/aid 对齐 → 字段证据与关系 → 显式表格/块清单覆盖 → 合并。
- JSON 合法和引文存在不能证明所有语义正确。否定句、跨企业错配、岗位地点串联都要测。
- 长文按段落/表格行/OCR 块拆分，不能静默截断；每个块记录处理状态，失败块不消失。
- 无 Key、鉴权失败、429、超时、截断、非法 JSON、漏项分开记录。失败不返回空列表冒充“非招聘”。
- 设整个运行的时间预算及模型修复次数/调用数上限，不无限重试，不自动换模型/服务。
- 正文/OCR/图片哈希、模型、提示词、Schema 变化时失效相关缓存；图片丢失也要检查。只做必要缓存，不建设缓存管理平台。
- 参考代码能配置任意 Base URL，这个能力不能照搬；带 Key 的生产请求固定到已确认地址，不跨域转发凭证。
- 首次真实调用前说明输入范围与预算。用户在本机填写 AGNES_API_KEY；已有明确授权时不重复询问。

## 7. 调度与发布

- 窗口固定为目标截止时间之前 86400 秒，实际启动延迟不移动它；明确首版时区支持范围。
- 在配置自然分钟认领当天任务，错过不补跑、失败不自动重新启动整窗；已开始任务可跨午夜收尾。
- 使用小型 SQLite 运行账本和跨进程保护；认领/完成/失败状态持久保存。只靠内存锁或 config_hash 不足以防重复。
- 只有确认原执行进程已退出，才把其已认领未完成的任务标为中断，不自动重新领取；第二实例不能把仍在正常执行的任务改成中断。单请求有限重试与整窗重跑分开。
- 调度扫描不能被报告生成阻塞。两份日报同分钟到期时，先分别冻结窗口并原子认领，再交给有数量上限的执行器；不能生成完招聘才检查练习是否到期。排队到次日仍未开始则标错过，不跨天补启动。
- 手动预览写 output/preview/<run_id>/，不占用和覆盖正式路径；刷新或双击不能无意重复创建昂贵任务。
- 正式报告保留 output/YYYY-MM-DD/招聘.html 和练习.html。
- 先写完整的不可变 JSON/资源版本，再生成绑定该版本的 HTML，最后替换固定 HTML 入口。不要把两次独立文件替换称为事务。
- 测试写资源后、写 HTML 前、发布后更新运行账本前的崩溃；半成品不能标已完成，恢复不能重复抓取/调用模型。
- 使用浏览器实际打开报告，检查图片相对路径、原文链接、筛选和错误提示。
- 程序要运行才能定时生成，电脑关机或进程退出不会继续；不自动注册开机任务，不掩盖这一限制。

## 8. 最小测试与完成标准

先建立约 12 个明确期望的代表性合成案例，按下面场景覆盖，数量不作为形式指标：

1. 起点入窗、终点排除；“昨天”相对时间与原文冲突；时间未知。
2. 账号顺序、关键词、同名近似账号、重复候选。
3. 一个账号失败、全部登录失效、真实无发现和部分成功。
4. 纯实习/社招/非招聘、混合校招表、多企业和岗位地点关系。
5. 原规格评级矩阵、未知分类/岗位、否定句。
6. 模型无 Key/超时/非法或截断 JSON/改来源/漏项。
7. OCR 局部失败、图片丢失、分块重复或冲突、必要缓存失效。
8. 恶意 HTML/危险 URL/图片跳内网；纯练习无 OCR/模型调用。
9. 双进程同窗、第二实例不干扰原进程、两份任务同分钟到期、重启、错过分钟、手动预览隔离、发布中断。

离线测试固定时钟、临时目录、假远端且禁止外网。浏览器检查用真实浏览器打开本地报告，不能仅检查 HTML 字符串。浏览器、OCR 权重和依赖下载属于环境准备，不在离线测试里偷偷联网。

真实验收至少要有：
- 一个招聘号和一个练习号有真实文章进入窗口；
- 普通正文招聘、需要 OCR 的招聘图片，以及应排除/待核实的样本；
- 人工对照企业、岗位、地点和来源，记录缺项，不只挑最好看的卡；
- 一次真实配置分钟运行、重复启动检查、一个来源失败、模型无 Key/超时降级；
- 干净环境依安装说明重跑离线样例，用户配置后可重复真实流程。

首版不建设 30 篇金标平台或质量仪表盘，不声称未经评估的 95% 召回或零幻觉。缩小工程范围不等于省略关键正确性测试。

运行实际 test、静态检查和 Python 编译检查，记录命令、退出码和通过/失败/跳过数。环境和文件存在后再给具体命令；不要无锁文件执行 --frozen，不要发明测试结果。关键用例不靠 skip/xfail 变绿。

## 9. 持续执行与交付

规则确认后维护一份简短 IMPLEMENTATION_STATUS.md，分别记录“已实现、离线验证、真实验证”，以及缺陷、证据和下一步。每个任务只有真实证据才能标通过。

每轮简短汇报：
- 当前阶段与实际改动；
- 检查命令及结果，哪些是真实/模拟；
- 未通过项及影响；
- 下一条可执行动作；
- 真正需要用户操作的事项。

上下文不足时写清状态再续做，不从头覆盖项目。没有工具、浏览器或权限时说明具体缺口，不能模拟工具输出。长命令后台运行并记录退出状态，继续独立工作。

交付源码、锁文件、空白配置、简单启动入口、样例报告、测试、验收记录、AGENTS.md、README.html。会话、真实账号名单、Key、图片、数据库、日志及报告默认不进 Git。

只有最小链路的离线检查、浏览器检查、真实双日报、OCR/模型、调度与故障演练均通过，才能称“第一版 MVP 完成”。不把第一版完成称为原完整产品规格全部完成。

现在开始读文件与 Git 状态；规则未确认先集中提交草稿，已确认则从 A 开始实施。不要再把 MCP、RSS、FastAPI、管理网站添加到第一版。
