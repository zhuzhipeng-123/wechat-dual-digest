# 给 GPT‑6 的无源码重建提示词

> 历史开发提示词：本轮第一版已收缩为最小双日报工具，请以 `IMPLEMENTATION_PROMPT.md` 和最新 `EXECUTION_PLAN.md` 执行。以下全量产品能力、立即创建全部服务及全产品验收门槛保留作背景，不作为第一版开发指令。

复制下方全部内容，并同时提供《微信公众号双日报项目：无源码重建规格》。**没有旧代码可看，也不需要旧代码。**

---

你要从零开发一个微信公众号双日报系统。你只能依据随附的《微信公众号双日报项目：无源码重建规格》和我后续的反馈工作；不存在可获取的旧仓库、旧提交、旧测试、旧数据库或旧凭证。不要要求我提供它们，也不要声称已经检查过旧代码。目标是复现规格中的可观察行为，而不是复刻内部实现。

先把规格转成可验证的需求清单与简洁架构，标出“必须遵守的行为”“可以自行决定的实现细节”“依赖外部服务而需现场验证的假设”。特别先验证微信公众平台登录/原文获取、微信读书登录/搜一搜发现、原文精确发布时间三条链路的可行性。若某条外部链路不可用，报告具体阻碍和可检验的替代方案，不要伪造已跑通的数据，也不要未经我确认改变“日报只用微信读书发现候选”的原则。

实现顺序：

1. 新建 Python 3.11+ 项目，初始化 Git，写项目级 `AGENTS.md` 和 `README.html`、依赖锁定文件、配置样例。先定义统一的文章与日报 JSON 数据契约、错误状态、时间窗口和安全 URL 规则。
2. 完成最小本地文章服务及两种扫码会话的接入；用自己的测试替身验证接口，待我在运行环境完成扫码后做真实可行性验证。凭证只存本地、不进仓库、不在日志或回复中输出。
3. 实现微信读书候选发现、原文抓取/精确时间入窗、正文净化、图片保存、缓存和练习日报。然后实现招聘的无模型降级报告，确保失败也可阅读并有唯一原文链接。
4. 实现本地 OCR、固定 Agnes API 的结构化事实抽取、Schema/来源/完整性校验、超长证据分段与合并、缓存失效、确定性 P0–P3 评级、企业自然段和固定 HTML 模板。模型不得补原文没有的事实、直接评级或生成 HTML；不得添加其他模型/CLI 回退。
5. 实现每窗口一次的调度与锁，再按规格补基础服务的 RSS、Markdown、管理界面、MCP 等第二阶段能力。不要让这些扩展功能改变日报的数据源约束。

每阶段都交付可运行、可评审的最小增量；未经我确认不要执行 `git add`、`git commit` 或 `git push`。函数保持简短清楚，关键边界有初学者能看懂的解释。写覆盖正常、边界和恶意输入的测试，固定测试时钟；运行测试、编译/静态检查，并记录实际结果。按照规格的“怎样测试”和“什么条件下才算开发完毕”逐项验收：离线零失败、真实双日报烟测、故障演练、基础服务逐项验证、新机器部署说明和最终验收记录都要有证据。不要把单元测试通过说成真实微信或 Agnes 已验证，也不要把“日报最小闭环可用”说成“整个项目完成”。真实登录、外部调用和发布前，明确列出所需的用户侧操作与访问的外部服务；不要向我索取或展示密钥、Cookie、完整账号配置和文章内容。

先回复一份实施计划，包含：可行性验证步骤、模块和数据契约、三个里程碑及各自验收标准、外部依赖风险、需要我亲自完成的扫码/配置动作。随后按计划自主推进能离线完成的工作；只有外部登录或确实无法自行判定的产品选择才请我参与。若规格中的某项无法在当前外部环境实现，请用证据解释影响，并给出最接近原行为且可验收的方案。最终请按规格列出每个完成门槛的“通过/失败/未验证”和证据；任何一项未通过时不要宣布整个项目开发完毕。
