# 微信公众号双日报项目：无源码重建规格

> 2026-09-19 最新范围：模型固定为 `agnes-2.5-flash`。用户进一步要求第一版是最小 MVP；首版仅实现双日报必要闭环，MCP、RSS、Markdown 导出、HTTP 服务/管理后台及通用文章平台能力后置。公众平台登录是否必要由真实原文抓取探针决定。本文件保留完整产品规格；首版范围、调用级分块/报告级分组约定与验收以最新 `EXECUTION_PLAN.md` 为准，开发执行见 `IMPLEMENTATION_PROMPT.md`。

> 本文供另一名开发者或 GPT‑6 **从零实现相近产品**。接收方没有原项目源码、数据库、历史缓存或运行凭证。文中的模块名和文件名是建议的交付接口，不要求复刻旧代码。本文不含真实公众号名单、密钥、Cookie、文章正文或历史报告。

## 1. 目标和复现边界

产品由两部分组成：①一个本地自托管的微信公众号文章服务，提供扫码登录、原文抓取，并可逐步提供搜索、文章列表、RSS、Markdown 导出、管理界面和 MCP；②在它之上按固定 24 小时窗口生成“招聘”和“练习”两份 HTML 日报。**优先确保双日报及其依赖的最小文章服务可用，再补齐基础服务的扩展功能。**

无源码意味着可复现的是**行为、数据契约、报告结构和失败处理**，不能保证页面像素、抓取条数、模型措辞与旧系统完全一致。微信读书搜索页面、微信公众平台登录/接口和文章可访问性会变化；在实现前必须先验证两种登录、候选发现和原文获取的技术可行性。搜一搜不提供可靠的完整性承诺，不能把“搜索结果为空”直接解释为“确实零文章”。

## 2. 系统边界和依赖

| 子系统 | 输入 | 输出与职责 |
| --- | --- | --- |
| 本地文章服务 | 用户自行完成的微信公众号后台扫码会话、微信原文 URL | 健康/登录状态；按 URL 返回标题、正文 HTML/纯文本、精确发布时间、图片等。最小接口建议为 `GET /api/health`、`GET /api/admin/status`、`POST /api/article`。 |
| 微信读书发现器 | 用户自行完成的微信读书扫码会话、公众号名及可选搜索词 | 只读搜一搜候选：公众号名、标题、原文 URL、页面相对时间、可用文章 ID。候选还不是“已入窗文章”。 |
| 日报管道 | 账号配置、调度配置、两种会话 | 依次发现、抓原文、核对窗口、净化正文、保存图片与快照、生成两份报告。 |
| 招聘事实抽取 | 本次原文纯文本及本地 OCR 文本 | 固定 JSON 结构的校招事实。外部模型只做事实抽取，脚本负责校验、评级、文案和 HTML。 |

建议技术栈：Python 3.11+、FastAPI、httpx、Playwright、BeautifulSoup、jsonschema、RapidOCR + ONNX Runtime CPU，前端使用原生 HTML/CSS/少量 JavaScript。可选的 RSS 数据库存储用 SQLite。具体库版本由新实现锁定并测试，不依赖旧仓库的版本号。

**两套独立会话：**微信公众平台登录供本地文章服务访问原文；微信读书登录供搜一搜发现候选。前者健康不代表后者有效，反之亦然。浏览器自动化需要真实可运行的 Chrome/Edge 或 Playwright Chromium；首次扫码需要可交互桌面环境。不要假定仅安装 Python 的 Playwright 包或启动 Docker 容器就能完成这一步。

## 3. 用户配置与最小接口

账号配置用 UTF‑8 文本，保持显式顺序。以下是**虚构占位示例**：

```text
[招聘]
示例就业号甲
示例就业号乙

[练习]
示例练习号甲 | 每日一题,成语积累 | 面试课
```

练习行是“公众号名 | 包含词列表 | 可选排除词列表”；逗号可为中英文，标题经全半角、括号和空白归一化后做包含匹配，任一包含词命中且无排除词命中才抓正文。招聘组不先按标题筛掉候选文章。配置错误要指明文件和行号；同组重名、空账号、练习行没有包含词均应报错。

调度配置至少有时区、两组任务的启用开关和 `HH:MM` 运行时间、招聘分析开关与总超时（建议默认 30 分钟）。模型配置须单独保存：Agnes Key、固定模型 `agnes-2.5-flash`、固定 HTTPS 地址 `https://apihub.agnes-ai.com/v1`。Key 只发给该地址；缺 Key、超时或 API 失败时不得切换其他模型或本地 CLI。账号配置和模型配置由新环境自己填写，文档不提供真实值。

最小文章接口应有明确契约：`POST /api/article` 接收 `{ "url": "https://mp.weixin.qq.com/s?..." }`，成功时返回可解析的标题、正文 HTML/纯文本、发布时间 Unix 时间戳、作者和图片来源；失败时给可分类错误。`GET /api/health` 只证明服务进程就绪；`GET /api/admin/status` 返回实际微信登录状态。接口的 JSON 外层包装可由新实现决定，但日报客户端必须与之匹配。所有外部 URL 都要限制协议和主机，不能把任意 URL 交给抓取器或图片下载器。

## 4. 固定时间窗口与发现规则

每个任务每天只处理一次连续 24 小时窗口：`[前一天的配置时刻, 当天的配置时刻)`；以 Unix 时间计算 86400 秒，再在配置时区展示，避免夏令时切换把长度改掉。调度器在配置的自然分钟触发，明确传入带时区的目标截止时间；执行启动延迟不能移动窗口。错过不补跑，失败不自动重试，旧日期任务不得跨天启动。同一任务/窗口使用跨进程锁避免重复执行，状态记录必须关联目标日期和精确窗口。

候选只从**已登录微信读书的搜一搜**获取。招聘按公众号名搜；练习可按“公众号名 + 每个包含词”分别搜并去重，只接受搜索结果中公众号名精确匹配的文章。页面“1小时前/昨天”等时间仅用于扩大后的候选范围过滤；正式入窗必须抓取微信原文并以原文发布时间判断 `start <= publish_time < end`。未知相对时间应保守抓正文复核。需要标记搜一搜页面错误、登录失效、搜索无结果和正文抓取失败，避免把它们混为零文章。

本地文章服务可对网络抖动、429、5xx 做有限重试与退避；鉴权失败、明确不可查看的原文和无效 URL 不应无限重试。账号按配置顺序串行，单篇失败不得阻断本账号后续文章，单账号失败不得阻断其他账号。重复候选按可靠 ID/安全原文 URL 去重，文章缓存必须与窗口和图片内容校验，不能用旧报告冒充当天结果。

## 5. 原文处理和证据保存

抓取后分别保存原始元数据、标题、原文链接、精确发布时间、净化后的 HTML、纯文本、图片清单与抓取状态。正文净化必须删除脚本、播放器、嵌入对象、事件属性和危险 URL；图片 URL 要限制允许的微信图片域名、重定向和大小，防止内网探测。保存图片时记录内容哈希、下载状态和错误；缓存命中要检查文件仍存在且哈希一致。

纯视频文章没有可核对正文时：招聘保守保留标题与唯一原文入口，并标“证据不足”；练习可标为忽略视频，不伪造练习内容。正文失败但候选时间落在窗口时保留标题、原文入口和失败诊断；候选仅因模糊相对时间靠近边界且正文失败时，不应强行判定它在窗口内。

## 6. 招聘日报：事实、校验、评级

### 6.1 识别边界

招聘组先抓窗口内全部文章，再判定当前开放的校园招聘全职机会。明确校招、秋招、春招、届别招聘、校招岗位标签或表格中“应届生”岗位可作为强证据；纯实习、纯社招、招聘会/宣讲会预告、就业指导、课程竞赛、招聘流程说明、拟录用/结果公示不算当前岗位。若旧结果公告里明确新增补招或开放投递，只处理新增机会。混合校招/社招表格只取对应校招行。不能因不偏好的岗位方向而删掉任何校招岗位。

### 6.2 模型输入输出契约

图片只在本机用 OCR 转文字；原图、本地绝对路径、Cookie 和 Key 不发给 Agnes。模型可见信息只限本次文章的公众号、文章 ID、标题、发布时间、原文链接、正文纯文本、OCR 文本和必要的分段上下文。每篇模型结果至少包含：

```json
{
  "aid": "input-article-id",
  "is_recruitment": true,
  "is_pure_internship": false,
  "companies": [{
    "company": "原文中的企业名",
    "employer_type": "性质待确认",
    "company_scale": "未确认",
    "classification_evidence": "",
    "job_names": ["原文岗位名"],
    "locations": ["原文地点"],
    "source_title": "逐字复制输入标题",
    "source_account": "逐字复制输入公众号",
    "source_publish_time": "逐字复制输入发布时间",
    "source_link": "逐字复制输入原文链接"
  }]
}
```

顶层按公众号分组，要求每个输入公众号/文章恰好输出一次，不得新增、漏掉或改来源。`employer_type` 仅可为“央国企/互联网企业/其他企业/性质待确认”；`company_scale` 仅可为“大型/中型/小型/未确认”。性质和规模只能使用**本次原文中可逐字核验的证据**，不能靠企业常识或联网猜测。同一篇同名企业合为一张卡，岗位和城市取去重并集；不明字段填“未提及/未确认”，不得自行补企业名称后缀、岗位或地点。纯实习或非目标文章的 `companies` 必须为空。

模型输出先做 JSON Schema 校验，再做来源绑定、事实一致性和显式标签/表格的反向完整性校验。长文和高密度汇总需按正文/OCR 证据分段，每段仍绑定文章 ID，最后合并；截断或漏项时缩小证据块重试，不能把未经验证的模型自由文本写入正式报告。网络或鉴权故障应在总预算内终止相应分析，已验证部分保留。正文、OCR、图片哈希或提示词/Schema 改变时旧分析失效。OCR 不完整、纯视频或关键图片缺失时宁可保留原文入口和“分析待补充”，也不伪造企业卡。

### 6.3 脚本固定评级

企业档位：`E0` = 原文明示**大型央国企**或**互联网企业**；`E1` = 其他已明确央国企或其他企业；`E?` = 性质待确认。城市档位：`T1` 北京/天津；`T2` 上海/南京/杭州/苏州；`T3` 郑州/西安；其他城市“其他”，没有可靠地点“未确认”。多地点取最优档位，但不能把街道名中的城市字样误认成工作城市。

| 企业档位 \ 城市档位 | T1 | T2 | T3 / 其他 / 未确认 |
| --- | --- | --- | --- |
| E0 | P0 | P1 | T3→P2；其他/未确认→P3 |
| E1 | P1 | P2 | P3 |
| E? | P2 | P3 | P3 |

目标方向包括算法、统计、数据、数字化、AI 等。**只有全部已知岗位名都明确属于销售/市场/行政等非目标方向时**强制降为 P3；岗位名称不明确不额外降级。岗位匹配文字、P 级和企业自然段都由脚本从已校验事实生成，模型不输出评级、推荐结论或 HTML。最终仍提醒以微信原文为准。

## 7. 练习日报与报告展示

练习只按标题包含词/排除词选择文章；命中后展示净化正文和完整可用图片，按公众号配置顺序、公众号内发布时间展示。不运行 OCR/AI，不补题、不解题、不改写题目或解析。所有账号抓取失败时页面明确写“失败/公众号抓取失败”及脱敏原因；只有账号搜索/抓取成功且确无命中时才写零命中。

两份报告分别输出 `output/YYYY-MM-DD/招聘.html`、`output/YYYY-MM-DD/练习.html`，顶部展示生成时间、时区、**完整窗口起止**、数据源/登录/分析状态和各阶段统计。招聘正文按公众号分组、文章按最高 P0–P3 排列，企业按“央国企/互联网企业/其他企业/性质待确认”分区，提供公众号/企业/城市/P 级筛选；只展示校招文章，但失败时保守保留可能漏判的原文入口。分析全不可用时统一“未评级/分析待补充”，报告仍可读；不要把零文章、数据源受阻、部分成功、模型失败混成一个状态。所有外部文本做 HTML 转义，原文链接只允许安全微信文章 URL，并且每篇保留唯一来源入口。招聘原图作为 OCR 证据保存，但不嵌入最终 HTML；练习图保留展示。

建议同时保存机器可读的逐日报告 JSON（含 `date/kind/window_start/window_end/source_status/analysis_status/accounts/stats/errors`）和逐篇内容/分析快照，用于续跑、审计和测试。运行目录、凭证、日志、图片及报告默认不进入版本控制。

## 8. 基础服务的第二阶段能力

完成双日报最小闭环后，继续实现完整产品的基础服务能力：公众号搜索和文章分页/关键词搜索、认证主体信息、图片代理、RSS 订阅/手动拉取/聚合源、SQLite 文章库、Markdown 增量导出、管理页、登录失效通知。MCP 提供六个带静态 Bearer Token 的本地工具：列订阅、搜账号、按时间游标取最近文章、读单篇 Markdown、订阅、取消订阅。它们不能绕过授权、URL 校验、限频或敏感信息保护。RSS/后台文章列表与日报的微信读书发现是不同入口；实现基础服务时不要悄悄把后台列表重新接入日报。

## 9. 验收用例与交付物

交付时应有可运行源码、锁定的依赖文件、空白配置样例、启动说明、单元/集成测试和固定模板报告。至少验证：①窗口左右边界及跨夏令时 86400 秒；②账号顺序、练习包含/排除词；③搜一搜相对时间只作候选、原文精确时间最终入窗；④搜索失败、登录失效、全部失败、部分失败、真实空窗口状态；⑤恶意 HTML/URL/图片重定向；⑥ OCR 局部失败、模型缺 Key/超时/非法 JSON/漏文章/漏岗位、缓存失效；⑦评级矩阵、岗位全保留；⑧两份 HTML 即使降级仍能阅读与追溯；⑨调度每窗口一次、错过不补跑。测试时间要显式固定，避免“1小时前”之类用例依赖运行当天。

建议按三个里程碑推进：A. 两种登录与文章发现/原文抓取的可行性验证；B. 练习日报和招聘无模型降级报告；C. OCR、Agnes 事实抽取、校验、评级与调度。每个里程碑给出实际运行记录与脱敏样例报告。真实端到端验收需要使用者在自己的环境登录并提供自己的配置；本文不提供也不能替代这些凭证。

### 怎样测试

1. **离线自动测试：**用固定时钟和本地假文章/假图片/假模型响应运行完整测试套件；必须覆盖上文九类用例，零失败、零跳过关键用例。再运行 Python 编译/静态检查和前端基本检查，记录命令、退出码、通过数。外部服务一律用替身，确保没有凭证也能重复运行。
2. **本机真实烟测：**用户在自己的机器完成两套扫码和 Agnes 配置。分别验证 API 健康与实际登录、微信读书对一个已知公众号能返回候选、本地文章服务能抓该候选的原文与精确发布时间。然后手动生成练习报告、招聘无模型报告、招聘完整分析报告；核对对应 JSON 的窗口、文章数、状态，抽查至少一篇文章的原文链接/发布时间、至少一张企业卡的岗位与地点均可在原文核实。空窗口不能替代有文章的烟测。
3. **故障演练：**在测试环境分别模拟一个公众号失败、全部公众号失败、正文失败、OCR 部分失败、Agnes 超时或无 Key，确认报告仍生成且状态/诊断准确；再运行一次调度器的配置分钟，确认同窗口不重复执行，错过的分钟不补跑。
4. **基础服务验收：**逐项调用搜索、文章列表/正文、图片代理、RSS 订阅与手动拉取、Markdown 增量导出、管理页和六个 MCP 工具；检查鉴权、分页/游标、错误响应与安全边界。只有页面出现或接口返回 200 不算通过，必须核对实际数据与操作结果。

### 什么条件下才算开发完毕

**阶段 B 通过只算“日报最小闭环可用”，不能称整个项目完成。** 整个项目完成必须同时满足：①第 1–8 节的功能已交付，包含基础服务第二阶段能力；②离线测试及安全检查全部通过，缺陷清单无未解决的阻断项；③至少一次使用真实登录态、真实原文、真实 OCR/Agnes 调用跑通双日报，并通过上述抽样核对；④调度与失败降级演练通过；⑤新机器可仅凭交付源码、配置样例与启动说明复现测试，用户自行扫码/填 Key 后可重复真实烟测。最终交付一份验收记录，逐项标明“通过/失败/未验证”、证据文件或脱敏截图、测试日期与环境。**外部链路未验证、只有模拟数据、或某项能力被推迟时，必须明确标为未完成或范围变更，不能宣称整个项目已完成。**
